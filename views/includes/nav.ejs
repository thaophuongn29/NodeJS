<nav class="navbar navbar-expand-sm navbar-dark bg-dark <% if(path === '/login') { %> justify-content-around <% } %>">
  <% if(!isAuthenticated && path === '/login') { %>
  <p class="text-white ">DANG NHAP</p>
  <% } %>
  <% if(isAuthenticated) { %>
  <div class="container-fluid">
    <a class="navbar-brand" href="<% if(isAuthenticated.user) { %>/<% } else { %>/confirm<% } %>"><i class="fa fa-home"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
      <ul class="navbar-nav">
        <% if(isAuthenticated.user) { %>
        <li class="nav-item">
          <a class="nav-link <% if(path==='/') { %> active <% } %>" href="/">Diem danh</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if(path==='/user') { %> active <% } %>" href="/user">Thong tin</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if(path==='/detail') { %> active <% } %>" href="/detail">Chi tiet</a>
        </li>
        <% } %>
        <% if (isAuthenticated.user || isAuthenticated.manager) { %>
        <li class="nav-item">
          <a class="nav-link <% if(path==='/covid') { %> active <% } %>" href="/covid">Dang ki covid</a>
        </li>
        <% } %>
        <% if (isAuthenticated.manager) { %>
        <li class="nav-item">
          <a class="nav-link <% if(path==='/confirm') { %> active <% } %>" href="/confirm">Xac nhan gio lam</a>
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if(isAuthenticated.user || isAuthenticated.manager) { %>
          <a class="nav-link <% if(path==='/logout') { %> active <% } %>" href="/logout">Logout</a>
          <% } else if (!isAuthenticated.user || !isAuthenticated.manager) { %>
          <a class="nav-link <% if(path==='/login') { %> active <% } %>" href="/login">Login</a>
          <% } %>
        </li>
      </ul>
    </div>
  </div>
  <% } %>
</nav>